services:
  - type: web
    name: ai-hrms-server
    env: node
    plan: free
    branch: main
    buildCommand: npm install --workspaces && npm run build
    startCommand: npm start
    # Let Render supply the runtime PORT via environment automatically.
